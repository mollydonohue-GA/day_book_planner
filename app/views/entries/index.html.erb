<p class="text-center">we are in index after auth.</p>

	<p><%= link_to("New Note", new_entry_path) %></p>


	<% @entries.each do |entry| %>

		<h2 class="text-center"><%= entry.subject %></h2>
		<p class="text-center"><%= entry.contents %></p>
		
	<% end %>

	<%= link_to "Log Out", destroy_user_session_path, method: :delete %>
